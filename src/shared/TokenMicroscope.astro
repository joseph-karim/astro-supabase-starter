<svg viewBox="0 0 720 200" role="img" aria-label="Dense vs diffuse token examples" style="width:100%;height:auto;filter:drop-shadow(0 2px 8px rgba(0,0,0,0.06))">
  <defs>
    <linearGradient id="boxGrad" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0%" stop-color="#ffffff"/>
      <stop offset="100%" stop-color="#f8fafc"/>
    </linearGradient>
    <linearGradient id="badgeGrad" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0%" stop-color="#10b981"/>
      <stop offset="100%" stop-color="#059669"/>
    </linearGradient>
    <style>
      .box{fill:url(#boxGrad);stroke:#2563eb;stroke-width:2}
      .box-fade{fill:#f8fafc;stroke:#cbd5e1;stroke-width:2}
      .dense{font:600 13px/1.5 -apple-system,BlinkMacSystemFont,'Inter',sans-serif;fill:#0f172a;letter-spacing:-0.01em}
      .diffuse{font:600 13px/1.5 -apple-system,BlinkMacSystemFont,'Inter',sans-serif;fill:#94a3b8;letter-spacing:-0.01em}
      .badge{fill:url(#badgeGrad)}
      .badge-text{font:700 11px -apple-system,BlinkMacSystemFont,'Inter',sans-serif;fill:#ffffff;letter-spacing:0.02em}
      .label{font:700 14px -apple-system,BlinkMacSystemFont,'Inter',sans-serif;fill:#0f172a;letter-spacing:-0.01em}
    </style>
  </defs>

  <g>
    <rect class="box" x="10" y="10" width="340" height="180" rx="8"/>
    <text class="label" x="20" y="35">Dense (quote-ready, ~30-40 tokens)</text>
    <text class="dense" x="20" y="60" style="font-size:11px">
      <tspan x="20" dy="0">"Answer-engine optimization means one</tspan>
      <tspan x="20" dy="16">canonical page with a ≤18-token claim, a</tspan>
      <tspan x="20" dy="16">table-backed proof, and JSON-LD so ChatGPT</tspan>
      <tspan x="20" dy="16">and Perplexity can cite you."</tspan>
    </text>
    <rect class="badge" x="220" y="155" width="100" height="20" rx="4"/>
    <text class="badge-text" x="270" y="169" text-anchor="middle">✓ Cited</text>
  </g>

  <g>
    <rect class="box-fade" x="370" y="10" width="340" height="180" rx="8"/>
    <text class="label" x="380" y="35">Diffuse (gets summarized, ~90+ tokens)</text>
    <text class="diffuse" x="380" y="60" style="font-size:11px">
      <tspan x="380" dy="0">"In today's rapidly evolving digital</tspan>
      <tspan x="380" dy="16">landscape, brands should consider multiple</tspan>
      <tspan x="380" dy="16">strategies to improve how AI systems might</tspan>
      <tspan x="380" dy="16">potentially surface their content..."</tspan>
    </text>
    <text class="diffuse" x="490" y="169" text-anchor="middle">No attribution</text>
  </g>
</svg>
