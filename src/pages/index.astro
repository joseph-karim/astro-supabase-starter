---
import JSONLD from '../shared/JSONLD.astro';
import OpenWindow from '../shared/OpenWindow.astro';
import LegacyVsAEO from '../shared/LegacyVsAEO.astro';
import Bullseye from '../shared/Bullseye.astro';
import TokenMicroscope from '../shared/TokenMicroscope.astro';
import PipelineDiagram from '../shared/PipelineDiagram.astro';
import AnswerCard from '../shared/AnswerCard.astro';
import ProofCard from '../shared/ProofCard.astro';
import ScoreDial from '../shared/ScoreDial.astro';
import GuaranteesStrip from '../shared/GuaranteesStrip.astro';

export const prerender = false;

const siteGraph = [
  {
    "@type": "Organization",
    "name": "Become The Answer",
    "url": "https://becometheanswer.ai",
    "logo": "https://becometheanswer.ai/og-logo.png",
    "sameAs": ["https://www.linkedin.com/company/becometheanswer"]
  },
  {
    "@type": "Service",
    "name": "AEO — AI Visibility & Conversion",
    "provider": {"@type":"Organization","name":"Become The Answer"},
    "description": "Claim → Cite → Close for challenger brands: claim page, evidence + schema, conversion-first design.",
    "areaServed": "Global",
    "url": "https://becometheanswer.ai"
  }
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Become the Answer — AEO: AI Visibility & Conversion for Challenger Brands</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="Own the claim. Win the click. Close the deal. Turn your expertise into answer-engine visibility that books meetings." />
    <link rel="canonical" href="https://becometheanswer.ai/" />
    <JSONLD graph={siteGraph} />
    <style>
      :root{
        --bg:#f8f9fa; --surface:#ffffff; --ink:#2c3e50; --muted:#546e7a; --accent:#4a6fa5;
        --highlight:#e3f2fd; --success:#4caf50; --border:#b0bec5; --shadow:rgba(44,62,80,0.08);
      }
      *{box-sizing:border-box}
      html,body{margin:0;padding:0;background:var(--bg);color:var(--ink);font:16px/1.6 system-ui,-apple-system,Segoe UI,Roboto,Inter,Helvetica,Arial}
      a{color:var(--accent);text-decoration:none}
      .wrap{max-width:1140px;margin:0 auto;padding:0 24px}
      .btn{display:inline-flex;gap:10px;align-items:center;border-radius:8px;padding:14px 24px;border:2px solid var(--accent);background:var(--accent);color:#ffffff;font-weight:700;transition:all .2s}
      .btn:hover{background:#3a5a85;border-color:#3a5a85}
      .btn--outline{background:transparent;color:var(--accent)}
      .btn--outline:hover{background:var(--highlight)}
      .ribbon{background:var(--highlight);border-bottom:1px solid var(--border);padding:12px 0;text-align:center;font-size:14px;color:var(--ink)}
      .hero{padding:60px 0 48px;background:linear-gradient(135deg, var(--surface) 0%, var(--highlight) 100%)}
      .hero-grid{display:grid;grid-template-columns:1.1fr .9fr;gap:40px;align-items:center}
      .h1{font-size:58px;line-height:1.1;margin:0 0 16px;font-weight:900;color:var(--ink)}
      .subhead{font-size:24px;color:var(--muted);margin:0 0 32px}
      .chips{display:flex;gap:10px;flex-wrap:wrap;margin:24px 0}
      .chip{padding:8px 16px;background:var(--surface);border:1px solid var(--border);border-radius:20px;font-size:13px;color:var(--muted)}
      .bar{display:flex;gap:12px;flex-wrap:wrap}
      .section{padding:60px 0;border-bottom:1px solid var(--border)}
      .kicker{font-size:12px;color:var(--accent);margin:0 0 8px;text-transform:uppercase;letter-spacing:.15em;font-weight:700}
      .h2{font-size:36px;font-weight:800;margin:0 0 16px;color:var(--ink)}
      .lead{font-size:18px;color:var(--muted);margin:0 0 24px;line-height:1.7}
      .card{background:var(--surface);border:1px solid var(--border);border-radius:12px;padding:24px;box-shadow:0 2px 8px var(--shadow)}
      .grid{display:grid;gap:32px}
      .two{grid-template-columns:repeat(2,1fr)}
      .three{grid-template-columns:repeat(3,1fr)}
      .list{margin:0;padding:0 0 0 24px;line-height:1.8}
      .list li{margin:10px 0}
      .price-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:24px;margin-top:32px}
      .price-card{padding:32px;border:2px solid var(--border);border-radius:12px;background:var(--surface)}
      .price-card.featured{border-color:var(--accent);box-shadow:0 4px 16px var(--shadow)}
      .price-card h3{margin:0 0 8px;font-size:22px;color:var(--ink)}
      .price-card .amt{font-size:32px;font-weight:800;margin:12px 0 24px;color:var(--accent)}
      .faq dt{font-weight:800;margin:24px 0 8px;font-size:18px;color:var(--ink)}
      .faq dd{margin:0 0 16px;color:var(--muted)}
      .footer{padding:32px 0;background:var(--surface);border-top:1px solid var(--border);color:var(--muted)}
      .footer-flex{display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:16px}
      .footer nav{display:flex;gap:20px}
      .footer a{color:var(--muted)}
      .footer a:hover{color:var(--accent)}
      @media (max-width:980px){
        .hero-grid,.two,.three,.price-grid{grid-template-columns:1fr}
        .h1{font-size:42px}
      }
    </style>
  </head>
  <body>
    <div class="ribbon">
      AEO is here: AI Overviews/AI Mode now sit on top of search, and brands are being judged by what AI answers say—not just where you rank.
    </div>

    <header class="hero">
      <div class="wrap hero-grid">
        <div>
          <h1 class="h1">Become the Answer.</h1>
          <p class="subhead">Own the claim. Win the click. Close the deal.</p>
          <div class="chips">
            <span class="chip">Consulting services</span>
            <span class="chip">Agencies</span>
            <span class="chip">Niche B2B SaaS/AI</span>
            <span class="chip">Professional influencers</span>
          </div>
          <div class="bar">
            <a href="/wizard" class="btn" data-cta>Get your Claim Dossier</a>
            <a href="/guarantees" class="btn btn--outline">See our Guarantees</a>
          </div>
        </div>
        <div>
          <OpenWindow />
        </div>
      </div>
    </header>

    <section class="section">
      <div class="wrap">
        <p class="kicker">Why the window is open</p>
        <h2 class="h2">The short version</h2>
        <p class="lead">Search is adding an AI answer layer (Google AI Overviews/AI Mode, ChatGPT, Perplexity). Those surfaces summarize the web and show a few citations. If you aren't the source they quote, you don't exist to the buyer—no matter how many "#1" rankings you used to hold.</p>

        <div class="grid three">
          <div class="card">
            <h3>1. Depth beats breadth</h3>
            <p>Answer engines reward specialized, info-dense expertise. Trying to cover "everything" makes you look like an aggregator.</p>
          </div>
          <div class="card">
            <h3>2. Quotes beat volume</h3>
            <p>LLMs prefer short, self-contained, information-dense sentences they can lift into an answer. Ten fluffy posts won't beat one precise page.</p>
          </div>
          <div class="card">
            <h3>3. Moving mainstream (fast)</h3>
            <p>Even analytics leaders now ship free AI-visibility tools, which means your competitors are getting their act together. Early movers become the canonical source models repeat.</p>
          </div>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="wrap">
        <p class="kicker">Clear contrast</p>
        <h2 class="h2">Legacy SEO vs AEO</h2>
        <LegacyVsAEO />
        <p style="margin-top:24px;color:var(--muted);font-size:14px;">
          <strong>Note:</strong> AI features show links to sources, but those links aren't a copy of top Google results—they're whatever best supports the answer. Ranking helps, but info-dense, quote-ready sources can leapfrog incumbents.
        </p>
      </div>
    </section>

    <section class="section">
      <div class="wrap">
        <p class="kicker">Who this is for</p>
        <h2 class="h2">And why you win</h2>
        <div class="grid two">
          <div>
            <ul class="list">
              <li><strong>Consulting services & agencies:</strong> You already have deep POVs and case evidence—AEO makes them extractable and repeatable.</li>
              <li><strong>Niche B2B SaaS/AI:</strong> You must own 1–2 definable concepts tied to your product.</li>
              <li><strong>Professional influencers:</strong> Your best lines get lost in long posts; we turn them into citable assets.</li>
            </ul>
            <p class="lead" style="margin-top:24px;"><strong>Principle:</strong> Focus over breadth. Too many topics pushes you into "generic aggregator" territory. Pick one niche idea and own it. Then another. Then another.</p>
          </div>
          <div class="card" style="display:flex;justify-content:center;align-items:center">
            <Bullseye />
          </div>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="wrap">
        <p class="kicker">Under the hood</p>
        <h2 class="h2">Just enough tech</h2>
        <p class="lead">How an LLM chooses you: Models build answers from short passages. Give them compact, self-contained lines and nearby proof. Think ≈60-token windows: dense enough to be useful, short enough to quote.</p>

        <div style="margin-top:32px">
          <TokenMicroscope />
        </div>

        <p style="margin-top:24px;color:var(--muted);">
          <strong>Why short wins:</strong> Short, scoped claims survive summarization and keep your name/URL attached; long, hedged paragraphs get compressed and credit drifts.
        </p>
      </div>
    </section>

    <section class="section">
      <div class="wrap">
        <p class="kicker">The play</p>
        <h2 class="h2">Claim → Cite → Close</h2>
        <div class="grid three">
          <div class="card">
            <h3>Claim</h3>
            <p>Pick one sharp idea (≤18 tokens), make it your H1, and put a 2-sentence TL;DR under it.</p>
          </div>
          <div class="card">
            <h3>Cite</h3>
            <p>Add a proof snapshot above the fold (table + tiny CSV) and JSON-LD (Org/Person/Article/Dataset) so engines can trust and align your identity.</p>
            <ProofCard />
          </div>
          <div class="card">
            <h3>Close</h3>
            <p>"Answer-ready up top; buyer-ready throughout." Put a clear CTA and scheduler above the fold.</p>
            <ScoreDial value={85} label="Cite→Close Score" />
          </div>
        </div>
        <div style="margin-top:32px">
          <PipelineDiagram />
        </div>
      </div>
    </section>

    <section class="section">
      <div class="wrap">
        <p class="kicker">Proof this isn't theoretical</p>
        <h2 class="h2">AEO is going mainstream</h2>
        <div class="grid three">
          <div class="card">
            <p>Mainstream platforms (Google AI Overviews/AI Mode) now surface AI answers with links; being the quoted source is the new distribution.</p>
          </div>
          <div class="card">
            <p>AEO is no longer fringe. Major players are publishing AEO guides and productizing it (Backlinko, Surfer, HubSpot).</p>
          </div>
          <div class="card">
            <p>Tooling is catching up. Amplitude launched AI Visibility with a free report—proof that measurement is going mainstream.</p>
          </div>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="wrap">
        <p class="kicker">What you get</p>
        <h2 class="h2">Outcomes, not fluff</h2>
        <div class="grid two">
          <div>
            <h3 style="margin-bottom:16px">One canonical "Claim Page" per idea:</h3>
            <ul class="list">
              <li>H1 ≤18 tokens, 2-sentence TL;DR, 3 pull-quotes</li>
              <li>Proof table + CSV download</li>
              <li>JSON-LD (Org/Person/Article/Dataset) validated</li>
              <li>Cross-post summary to Substack/Medium + dataset to GitHub</li>
              <li>Screenshots of "appeared/cited" tests, not hand-wavy reports</li>
            </ul>
          </div>
          <div class="card">
            <h3 style="margin-bottom:16px">Measurement we care about:</h3>
            <p><strong>AAQM</strong> (Answer-Attributed Qualified Meetings) and <strong>AAP($)</strong> (pipeline).</p>
            <p style="margin-top:16px;color:var(--muted);">Citations are inputs; meetings are outcomes.</p>
            <AnswerCard />
          </div>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="wrap">
        <p class="kicker">Productized, simple</p>
        <h2 class="h2">Packages</h2>
        <div class="price-grid">
          <div class="price-card">
            <h3>BTA-01 — Claim Kit</h3>
            <div class="amt">$1,500</div>
            <p style="color:var(--muted);margin-bottom:20px">5 days</p>
            <ul class="list" style="font-size:14px">
              <li>H1/TL;DR/quotes</li>
              <li>Proof table + CSV</li>
              <li>JSON-LD, CTA copy</li>
              <li>Design wire</li>
            </ul>
            <div class="bar" style="margin-top:24px">
              <a href="/wizard" class="btn" data-cta>Order Claim Kit</a>
            </div>
          </div>
          <div class="price-card featured">
            <h3>BTA-02 — Challenger Sprint</h3>
            <div class="amt">$7,500</div>
            <p style="color:var(--muted);margin-bottom:20px">3 weeks</p>
            <ul class="list" style="font-size:14px">
              <li>Publish the page; cross-post</li>
              <li>Answer tests with screenshots</li>
              <li>Wire CTA & analytics</li>
              <li>Draft 10 curator emails</li>
            </ul>
            <div class="bar" style="margin-top:24px">
              <a href="/wizard" class="btn" data-cta>Book the Sprint</a>
            </div>
          </div>
          <div class="price-card">
            <h3>BTA-03 — Compound Plan</h3>
            <div class="amt">$2,500/mo</div>
            <p style="color:var(--muted);margin-bottom:20px">Ongoing</p>
            <ul class="list" style="font-size:14px">
              <li>1 micro-update + 1 support post/month</li>
              <li>Weekly tests + 3 curator follow-ups</li>
              <li>Monthly AAQM/AAP report</li>
            </ul>
            <div class="bar" style="margin-top:24px">
              <a href="/wizard" class="btn btn--outline">Start compounding</a>
            </div>
          </div>
        </div>
        <p style="text-align:center;margin-top:32px;color:var(--muted)">Start with the Ownable Idea Wizard (your Wizard fee is credited to Sprint).</p>
      </div>
    </section>

    <section class="section">
      <div class="wrap">
        <p class="kicker">Specific, real</p>
        <h2 class="h2">Risk reversals</h2>
        <GuaranteesStrip />
        <div class="grid three" style="margin-top:32px">
          <div class="card">
            <h3>Fit-or-Free (Wizard)</h3>
            <p>If we can't produce 3 claims scoring ≥85 with 5+ credible sources for the top pick, we refund the Wizard fee. You keep the Dossier.</p>
          </div>
          <div class="card">
            <h3>Appearance Guarantee (Sprint)</h3>
            <p>If you don't get at least one "appeared or cited" screenshot on a seeded prompt within 4 weeks, we work one extra week free and add an extra proof asset.</p>
          </div>
          <div class="card">
            <h3>Meeting-Backed Credit (Compound)</h3>
            <p>If AAQM = 0 after 60 days and you approved changes within 48 hours, we credit the next month.</p>
          </div>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="wrap">
        <p class="kicker">FAQ</p>
        <h2 class="h2">Straight answers</h2>
        <dl class="faq">
          <dt>How is this different from SEO?</dt>
          <dd>Legacy SEO optimizes for lists of links. AEO optimizes for being the source AI answers cite—and making that citation click.</dd>

          <dt>What's the typical timeline?</dt>
          <dd>You'll usually see early movement in 2–6 weeks if you have any authority at all. We show before/after screenshots.</dd>

          <dt>Do we need to produce more content?</dt>
          <dd>No. You need one canonical page per idea, with proof near the top. Then keep it fresh monthly.</dd>
        </dl>
      </div>
    </section>

    <section class="section" style="border-bottom:none">
      <div class="wrap">
        <div class="card" style="display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:24px;padding:40px">
          <div>
            <h2 class="h2" style="margin:0 0 12px">Own the claim. Win the click. Close the deal.</h2>
            <p style="color:var(--muted);margin:0">We optimize for meetings, not vanity citations.</p>
          </div>
          <a href="/wizard" class="btn" data-cta style="flex-shrink:0">Get your Claim Dossier</a>
        </div>
      </div>
    </section>

    <footer class="footer">
      <div class="wrap footer-flex">
        <small>© {new Date().getFullYear()} Become The Answer</small>
        <nav>
          <a href="/wizard">Services</a>
          <a href="/claim/18-token-rule">Resources</a>
          <a href="/guarantees">Guarantees</a>
        </nav>
      </div>
    </footer>

    <script>
      const ref = document.referrer || '';
      fetch('/api/events', {
        method: 'POST',
        headers: { 'content-type': 'application/json' },
        body: JSON.stringify({
          claim_slug: 'home',
          page: '/',
          referrer: ref,
          event_type: 'session'
        })
      }).catch(() => {});

      document.querySelectorAll('[data-cta]').forEach(el => {
        el.addEventListener('click', () => {
          fetch('/api/events', {
            method: 'POST',
            headers: { 'content-type': 'application/json' },
            body: JSON.stringify({
              claim_slug: 'home',
              page: '/',
              event_type: 'cta_click'
            })
          }).catch(() => {});
        });
      });
    </script>
  </body>
</html>
