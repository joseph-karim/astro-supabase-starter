---
import JSONLD from '../shared/JSONLD.astro';
import AnswerBeam from '../shared/AnswerBeam.astro';
import PipelineFlow from '../shared/PipelineFlow.astro';
import TargetRings from '../shared/TargetRings.astro';
import TokenComparison from '../shared/TokenComparison.astro';
import ConversationArc from '../shared/ConversationArc.astro';

export const prerender = false;

const siteGraph = [
  {
    "@type": "Organization",
    "name": "Become The Answer",
    "url": "https://becometheanswer.ai",
    "logo": "https://becometheanswer.ai/og-logo.png",
    "sameAs": ["https://www.linkedin.com/company/becometheanswer"]
  },
  {
    "@type": "Service",
    "name": "AEO — AI Visibility & Conversion",
    "provider": {"@type":"Organization","name":"Become The Answer"},
    "description": "Claim → Cite → Close for challenger brands: claim page, evidence + schema, conversion-first design.",
    "areaServed": "Global",
    "url": "https://becometheanswer.ai"
  }
];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Become the Answer — AI Visibility, Content Operations & Conversion Services</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="Be the person buyers trust—and the page they click when it's time to choose. AI Visibility, Content Operations, and Conversion services." />
    <link rel="canonical" href="https://becometheanswer.ai/" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@700;800&family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <JSONLD graph={siteGraph} />
    <style>
      :root{
        --violet-900:#1A1033;--violet-800:#241445;--violet-700:#39206B;--violet-600:#5A3AF5;--violet-500:#7A5CFF;--violet-400:#A08BFF;
        --teal-500:#31D0C6;--magenta-500:#FF4D9D;
        --bg-950:#0A0B0D;--bg-900:#101322;--panel-800:#111319;--stroke-700:#1B1F29;
        --ink-100:#F3F1FF;--ink-300:#D7D2FF;--ink-500:#9EA3C7;
        --ok:#20C997;--warn:#FFB02E;--danger:#FF6B6B;
        --grad-answer:linear-gradient(135deg,var(--violet-600) 0%,var(--violet-400) 55%,var(--teal-500) 100%);
        --grad-iris:linear-gradient(160deg,#2B1A47 0%,#39206B 60%,#5A3AF5 100%);
        --shadow:0 10px 30px rgba(0,0,0,.35);
        --font-h:'Space Grotesk',ui-sans-serif,system-ui;
        --font-b:'Inter',ui-sans-serif,system-ui;
      }
      *{box-sizing:border-box;margin:0;padding:0}
      html{scroll-behavior:smooth}
      body{margin:0;padding:0;background:var(--bg-950);color:var(--ink-300);font:17px/1.6 var(--font-b);-webkit-font-smoothing:antialiased;-moz-osx-font-smoothing:grayscale}
      a{color:var(--teal-500);text-decoration:none;transition:all 0.2s cubic-bezier(.2,.6,.2,1)}
      a:hover{color:var(--ink-100)}
      .wrap{max-width:1120px;margin:0 auto;padding:0 24px}
      .btn{display:inline-flex;gap:10px;align-items:center;padding:14px 24px;border-radius:12px;border:1px solid transparent;font:700 15px var(--font-b);letter-spacing:-0.01em;transition:all 0.2s cubic-bezier(.2,.6,.2,1);cursor:pointer}
      .btn-primary{background:var(--grad-answer);color:#0B0C10;box-shadow:0 0 0 0 rgba(122,92,255,0.4)}
      .btn-primary:hover{filter:brightness(1.1);box-shadow:0 0 0 8px rgba(122,92,255,0.15);transform:translateY(-2px)}
      .btn-secondary{background:transparent;border-color:var(--stroke-700);color:var(--ink-100)}
      .btn-secondary:hover{border-color:var(--violet-500);background:rgba(122,92,255,0.1)}
      .ribbon{background:var(--grad-iris);border-bottom:1px solid var(--stroke-700);padding:14px 0;text-align:center;font-size:14px;line-height:1.5;color:var(--ink-300);font-weight:500}
      .hero{padding:80px 0 60px;background:var(--bg-950);position:relative;overflow:hidden;border-bottom:1px solid var(--stroke-700)}
      .hero::before{content:'';position:absolute;top:-200px;right:-200px;width:600px;height:600px;background:radial-gradient(circle,rgba(90,58,245,0.15) 0%,transparent 70%);border-radius:50%;animation:pulse 12s ease-in-out infinite}
      @keyframes pulse{0%,100%{transform:scale(1) rotate(0deg);opacity:0.4}50%{transform:scale(1.2) rotate(180deg);opacity:0.6}}
      .hero-grid{display:grid;grid-template-columns:1.1fr .9fr;gap:50px;align-items:center;position:relative;z-index:1}
      .h1{font:800 56px/1.05 var(--font-h);letter-spacing:-0.02em;color:var(--ink-100);margin:0 0 16px}
      .h2{font:800 40px/1.15 var(--font-h);color:var(--ink-100);letter-spacing:-0.02em;margin:0 0 16px}
      .h3{font:700 24px/1.3 var(--font-h);color:var(--ink-100);margin:0 0 12px}
      .subhead{font:500 24px/1.4 var(--font-b);color:var(--ink-300);margin:0 0 12px;letter-spacing:-0.01em}
      .kicker{font:700 12px/1.5 var(--font-b);letter-spacing:.14em;text-transform:uppercase;color:var(--ink-500);margin:0 0 10px}
      .lead{font:400 18px/1.7 var(--font-b);color:var(--ink-300);margin:0 0 24px;max-width:800px}
      .chips-label{font:600 13px var(--font-b);letter-spacing:0.05em;text-transform:uppercase;color:var(--teal-500);margin:0 0 12px;display:flex;align-items:center;gap:8px}
      .chips-label::before{content:'';width:20px;height:2px;background:var(--grad-answer);border-radius:999px}
      .chips{display:flex;gap:10px;flex-wrap:wrap;margin:0 0 32px}
      .chip{display:inline-flex;align-items:center;gap:8px;padding:10px 18px;border-radius:999px;background:#0E1118;border:1px solid var(--stroke-700);color:#CFE9E7;font-size:14px;font-weight:600;transition:all 0.3s cubic-bezier(.2,.6,.2,1);position:relative;overflow:hidden}
      .chip::before{content:'';position:absolute;top:0;left:-100%;width:100%;height:100%;background:linear-gradient(90deg,transparent,rgba(49,208,198,0.2),transparent);transition:left 0.5s ease}
      .chip:hover{border-color:var(--teal-500);background:rgba(49,208,198,0.15);transform:translateY(-2px) scale(1.05);box-shadow:0 4px 12px rgba(49,208,198,0.2)}
      .chip:hover::before{left:100%}
      .bar{display:flex;gap:12px;flex-wrap:wrap;margin:24px 0}
      .section{padding:80px 0;border-bottom:1px solid var(--stroke-700)}
      .card{background:var(--panel-800);border:1px solid var(--stroke-700);border-radius:16px;padding:24px;transition:all 0.3s cubic-bezier(.2,.6,.2,1);box-shadow:0 1px 0 var(--stroke-700) inset}
      .card:hover{border-color:var(--violet-600);box-shadow:0 1px 0 var(--stroke-700) inset,var(--shadow);transform:translateY(-4px)}
      .grid{display:grid;gap:32px}
      .two{grid-template-columns:repeat(2,1fr)}
      .three{grid-template-columns:repeat(3,1fr)}
      .list{margin:0;padding:0 0 0 24px;line-height:1.9}
      .list li{margin:12px 0;color:var(--ink-300)}
      .list li strong{color:var(--ink-100);font-weight:600}
      .price-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:24px;margin-top:32px}
      .price-card{padding:32px;border:2px solid var(--stroke-700);border-radius:16px;background:var(--panel-800);transition:all 0.3s ease;position:relative}
      .price-card:hover{border-color:var(--violet-500);transform:translateY(-6px);box-shadow:var(--shadow)}
      .price-card.featured{border-color:var(--violet-600);background:var(--grad-iris)}
      .price-card.featured::before{content:'MOST POPULAR';position:absolute;top:16px;right:-30px;background:var(--grad-answer);color:#0B0C10;padding:4px 40px;font-size:10px;font-weight:700;letter-spacing:0.1em;transform:rotate(45deg)}
      .price-card h3{margin:0 0 8px;font-size:22px;color:var(--ink-100);font-weight:700}
      .price-card .amt{font-size:36px;font-weight:800;margin:12px 0 24px;background:var(--grad-answer);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
      .faq dt{font-weight:700;margin:24px 0 8px;font-size:18px;color:var(--ink-100)}
      .faq dd{margin:0 0 16px;color:var(--ink-300);line-height:1.7}
      .footer{padding:40px 0;background:var(--bg-900);border-top:1px solid var(--stroke-700);color:var(--ink-500)}
      .footer-flex{display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:16px}
      .footer nav{display:flex;gap:20px}
      .footer a{color:var(--ink-500);font-weight:500;transition:color 0.2s ease}
      .footer a:hover{color:var(--teal-500)}
      .divider{height:6px;background:var(--grad-answer);border-radius:999px;margin:24px 0}
      table{border-collapse:collapse;font-size:15px;width:100%}
      table thead tr{background:var(--grad-iris)}
      table th{padding:16px;text-align:left;border:1px solid var(--stroke-700);font-weight:600;color:var(--ink-100)}
      table td{padding:14px;border:1px solid var(--stroke-700);color:var(--ink-300);line-height:1.6}
      table tbody tr{background:var(--panel-800);transition:background 0.2s ease}
      table tbody tr:hover{background:rgba(90,58,245,0.1)}
      table tbody tr td strong{color:var(--ink-100);font-weight:600}
      @media(prefers-reduced-motion:reduce){*{animation:none !important;transition:none !important}}
      @media(max-width:980px){
        .hero-grid,.two,.three,.price-grid{grid-template-columns:1fr}
        .h1{font-size:42px}
        .h2{font-size:32px}
        .subhead{font-size:20px}
        .hero{padding:50px 0 40px}
        .section{padding:50px 0}
      }
    </style>
  </head>
  <body>
    <div class="ribbon">
      AEO is here: AI answers now sit on top of search. Buyers are judged by what AI says—and who it cites.
    </div>

    <header class="hero">
      <div class="wrap hero-grid">
        <div>
          <p class="kicker">AEO — AI VISIBILITY, CONTENT OPERATIONS & CONVERSION SERVICES</p>
          <h1 class="h1">Become the Answer.</h1>
          <p class="subhead">Be the person buyers trust—and the page they click when it's time to choose.</p>
          <div class="bar">
            <a href="#services" class="btn btn-primary" data-cta>Talk to us</a>
            <a href="/guarantees" class="btn btn-secondary">See our Guarantees</a>
          </div>
          <p class="chips-label">Tailor Made For</p>
          <div class="chips">
            <span class="chip">Consulting services</span>
            <span class="chip">Agencies</span>
            <span class="chip">B2B SaaS/AI</span>
            <span class="chip">Professional Creators</span>
          </div>
        </div>
        <div>
          <AnswerBeam />
        </div>
      </div>
    </header>

    <section class="section">
      <div class="wrap">
        <div style="text-align:center;margin-bottom:48px">
          <PipelineFlow />
        </div>
      </div>
    </section>

    <section class="section">
      <div class="wrap">
        <h2 class="h2">Why this matters now</h2>
        <p class="lead">People don't type one keyword anymore. They ask AI a <strong>long, specific question</strong> with context and constraints. The model answers and shows <strong>a few sources</strong>. If you're not the one it cites, you're invisible—even with great Google rankings.</p>

        <div class="card" style="margin:32px 0">
          <h3 class="h3">What changed</h3>
          <ul class="list">
            <li><strong>Search:</strong> many short queries; BoF keywords are crowded and pricey.</li>
            <li><strong>LLMs:</strong> one ongoing conversation; the model consolidates and <strong>pins 1–3 sources</strong>.</li>
            <li><strong>Your goal:</strong> be the cited source when buyers ask, "<strong>Given my constraints, who should I pick—and why?</strong>"</li>
          </ul>
        </div>

        <ConversationArc />
      </div>
    </section>

    <section class="section">
      <div class="wrap">
        <p class="kicker">Commercial Intent</p>
        <h2 class="h2">Legacy SEO vs AEO</h2>
        <div style="overflow-x:auto;margin:24px 0">
          <table>
            <thead>
              <tr>
                <th></th>
                <th>Legacy SEO</th>
                <th style="background:var(--grad-answer);color:#0B0C10">AEO (Answer-Engine Optimization)</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td style="font-weight:700;color:var(--ink-100)">Buyer behavior</td>
                <td>Many queries → compare links</td>
                <td><strong>One conversation → asks for a verdict</strong></td>
              </tr>
              <tr>
                <td style="font-weight:700;color:var(--ink-100)">Your job</td>
                <td>Rank broadly</td>
                <td><strong>Be the quoted solution</strong> for a narrow question</td>
              </tr>
              <tr>
                <td style="font-weight:700;color:var(--ink-100)">Content</td>
                <td>Topic clusters, long intros</td>
                <td><strong>One canonical page per idea</strong></td>
              </tr>
              <tr>
                <td style="font-weight:700;color:var(--ink-100)">Structure</td>
                <td>Walls of text</td>
                <td>≤18-word claim + <strong>proof near the top</strong></td>
              </tr>
              <tr>
                <td style="font-weight:700;color:var(--ink-100)">Proof</td>
                <td>Opinions</td>
                <td><strong>Table + tiny CSV + JSON-LD (entity clean)</strong></td>
              </tr>
              <tr>
                <td style="font-weight:700;color:var(--ink-100)">Metric</td>
                <td>Impressions, rank</td>
                <td><strong>Answer-Attributed Meetings (AAQM) & pipeline</strong></td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>
    </section>

    <section class="section" id="services">
      <div class="wrap">
        <p class="kicker">Services (three levels)</p>
        <h2 class="h2">1) AI Visibility — Become the cited source</h2>
        <p class="lead">Make your best ideas <strong>quote-ready</strong> and <strong>click-worthy</strong> so AI answers point to you.</p>

        <div class="grid two" style="margin-top:32px">
          <div>
            <h3 class="h3">Outcomes</h3>
            <ul class="list">
              <li>One canonical <strong>Answer Page</strong> per idea (≤18-word claim, proof up top, JSON-LD)</li>
              <li>Early <strong>"appeared/cited"</strong> screenshots on seeded buyer questions</li>
            </ul>

            <h3 class="h3" style="margin-top:32px">Deliverables (10 days)</h3>
            <ul class="list">
              <li>Tightened <strong>headline ≤18</strong>, TL;DR, and 3 pull-quotes</li>
              <li><strong>Proof table + tiny CSV</strong> (we build from your materials)</li>
              <li><strong>JSON-LD</strong> (Org/Person/Article/Dataset) validated</li>
              <li>Cross-post summary (Substack/Medium) + dataset (GitHub)</li>
              <li>Before/after screenshots + next actions</li>
            </ul>

            <div class="bar" style="margin-top:24px">
              <a href="#pricing" class="btn btn-primary" data-cta>Get AI Visibility</a>
            </div>
          </div>
          <div class="card" style="display:flex;justify-content:center;align-items:center;min-height:400px">
            <TargetRings />
          </div>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="wrap">
        <h2 class="h2">2) Content Operations — Ship more, every week, everywhere</h2>
        <p class="lead">Stand up a <strong>weekly content machine</strong> that turns one recording into posts, clips, and pages—<strong>all pointing to your Answer Pages</strong>.</p>

        <div class="card" style="margin:32px 0">
          <h3 class="h3">Outcomes</h3>
          <ul class="list">
            <li>A repeatable weekly cadence that compounds familiarity <strong>and</strong> feeds AEO</li>
            <li>Multi-platform distribution (site, newsletter, LinkedIn/X, YouTube/Shorts)</li>
          </ul>

          <h3 class="h3" style="margin-top:32px">Deliverables (3 weeks)</h3>
          <ul class="list">
            <li>Boards: <strong>Guest/Topic CRM</strong> + <strong>Content board</strong> (Notion/Airtable)</li>
            <li>Automation (n8n/Lindy): <strong>Transcribe → Nugget → Clips → Posts → Publish</strong></li>
            <li><strong>Episode/Article page</strong> templates (Astro) with JSON-LD</li>
            <li>First batch shipped: 1 long post, 1 short post, <strong>5–10 clips</strong>, Answer Page update</li>
            <li>Analytics bucket: <strong>AAS / EIR / CMR</strong> ("AI Answers" channel)</li>
          </ul>
        </div>

        <div class="card" style="margin:32px 0;background:var(--grad-iris)">
          <h3 class="h3">Weekly rhythm (what you'll actually do)</h3>
          <ul class="list">
            <li><strong>Record once</strong> (30–45 min)</li>
            <li>Approve <strong>nuggets/clips/posts</strong></li>
            <li>Update <strong>one Answer Page</strong></li>
            <li>Distribute to <strong>multiple channels</strong></li>
          </ul>
        </div>

        <div class="bar">
          <a href="#pricing" class="btn btn-primary" data-cta>Set up my Content OS</a>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="wrap">
        <h2 class="h2">3) Conversion & Agentic Funnel — All-bound engagement</h2>
        <p class="lead">Tie it together: <strong>inbound + de-anonymization + triggered outbound</strong> that only fires for <strong>fit + intent</strong>.</p>

        <div class="grid two" style="margin-top:32px">
          <div>
            <h3 class="h3">Outcomes</h3>
            <ul class="list">
              <li>Visitors from answers and content turn into <strong>booked meetings</strong></li>
              <li>Founder-friendly outreach that references what people actually watched/read</li>
            </ul>

            <h3 class="h3" style="margin-top:32px">Deliverables (setup + 1st month)</h3>
            <ul class="list">
              <li>De-anon + enrichment (company-level) and <strong>fit scoring</strong></li>
              <li><strong>Triggers:</strong> episode → Answer Page → pricing + ≥45s watch or 2 scrolls</li>
              <li>Drafted <strong>founder DMs</strong> (episode-led) + <strong>SDR 3-step</strong> sequence (value-first)</li>
              <li>Light <strong>retargeting</strong> to Answer Page viewers</li>
              <li>AAQM/AAP dashboard + "iterate/kill" rules</li>
            </ul>

            <div class="bar" style="margin-top:24px">
              <a href="#pricing" class="btn btn-primary" data-cta>Build my Conversion Engine</a>
            </div>
          </div>
          <div class="card" style="display:flex;justify-content:center;align-items:center;min-height:400px">
            <AnswerBeam />
          </div>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="wrap">
        <h2 class="h2">Under the hood (just enough to win the verdict)</h2>
        <p class="lead">LLMs lift <strong>short, self-contained, information-dense</strong> lines.</p>
        <div style="margin-top:32px">
          <TokenComparison />
        </div>
        <p style="margin-top:24px;color:var(--ink-300);">
          <strong style="color:var(--ink-100)">Why it matters:</strong> Dense lines <strong>keep your name attached</strong>, a link appears, and a <strong>clear CTA</strong> converts.
        </p>
      </div>
    </section>

    <section class="section">
      <div class="wrap">
        <h2 class="h2">What we measure (no vanity)</h2>
        <div class="grid three" style="margin-top:32px">
          <div class="card">
            <h3 class="h3" style="color:var(--teal-500)">AAQM</h3>
            <p><strong>Answer-Attributed Qualified Meetings</strong> (North Star)</p>
          </div>
          <div class="card">
            <h3 class="h3" style="color:var(--teal-500)">AAP($)</h3>
            <p><strong>Pipeline from AAQM</strong></p>
          </div>
          <div class="card">
            <h3 class="h3" style="color:var(--teal-500)">AAS / EIR / CMR</h3>
            <p><strong>AI-sessions / Engaged-intent rate / CTA→Meeting</strong></p>
          </div>
        </div>

        <div class="card" style="margin:32px 0;background:rgba(255,107,107,0.1);border-color:#FF6B6B">
          <h3 class="h3" style="color:#FF6B6B">Brakes (when to change course)</h3>
          <ul class="list">
            <li>AAS &lt; 50 <strong>or</strong> CMR &lt; 3% (30 days) → rewrite first 100 words + tighten the claim</li>
            <li>AAQM = 0 with traffic (60 days) → change theme or guest mix</li>
          </ul>
        </div>
      </div>
    </section>

    <section class="section" id="pricing">
      <div class="wrap">
        <p class="kicker">Packages & Pricing</p>
        <h2 class="h2">Three service levels</h2>
        <div class="price-grid">
          <div class="price-card">
            <h3>AI Visibility (Level 1)</h3>
            <div class="amt">$3,000</div>
            <p style="color:var(--ink-500);margin-bottom:20px">10 days</p>
            <ul class="list" style="font-size:14px">
              <li>Claim tighten</li>
              <li>Proof table + CSV</li>
              <li>JSON-LD</li>
              <li>Cross-posts</li>
              <li>Screenshots</li>
            </ul>
            <div class="bar" style="margin-top:24px">
              <a href="#services" class="btn btn-primary" data-cta>Get AI Visibility</a>
            </div>
          </div>
          <div class="price-card featured">
            <h3>Content Operations (Level 2)</h3>
            <div class="amt">$7,500</div>
            <p style="color:var(--ink-300);margin-bottom:20px">3 weeks</p>
            <ul class="list" style="font-size:14px">
              <li>Boards + automations</li>
              <li>Page templates</li>
              <li>First batch shipped</li>
              <li>Analytics wired</li>
            </ul>
            <div class="bar" style="margin-top:24px">
              <a href="#services" class="btn btn-primary" data-cta>Set up my Content OS</a>
            </div>
          </div>
          <div class="price-card">
            <h3>Conversion & Agentic Funnel (Level 3)</h3>
            <div class="amt">$12,000</div>
            <p style="color:var(--ink-500);margin-bottom:6px">setup</p>
            <div class="amt" style="font-size:24px;margin:0 0 20px">+ $3,000/mo</div>
            <ul class="list" style="font-size:14px">
              <li>De-anon + triggers</li>
              <li>Founder DM + SDR sequence</li>
              <li>Retargeting</li>
              <li>AAQM/AAP dashboard</li>
            </ul>
            <div class="bar" style="margin-top:24px">
              <a href="#services" class="btn btn-primary" data-cta>Build my Conversion Engine</a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="wrap">
        <p class="kicker">Guarantees</p>
        <h2 class="h2">We de-risk the outcome</h2>
        <div class="grid three" style="margin-top:32px">
          <div class="card">
            <h3 class="h3" style="color:var(--ok)">Fit-or-Free (Level 1)</h3>
            <p>If we can't produce <strong>3 strong claims with credible proof</strong>, refund.</p>
          </div>
          <div class="card">
            <h3 class="h3" style="color:var(--ok)">Appearance (Level 2)</h3>
            <p>At least one <strong>"appeared or cited"</strong> screenshot on a seeded question in 4 weeks—or an extra week free + added proof.</p>
          </div>
          <div class="card">
            <h3 class="h3" style="color:var(--ok)">Meeting-Backed Credit (Level 3)</h3>
            <p><strong>AAQM = 0</strong> after 60 days (with timely approvals) → credit next month.</p>
          </div>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="wrap">
        <p class="kicker">FAQ</p>
        <h2 class="h2">Straight answers</h2>
        <dl class="faq">
          <dt>Do we need lots of content?</dt>
          <dd>Yes—<strong>one recording a week</strong>, atomized into posts, clips, and pages that all point to your Answer Pages.</dd>

          <dt>Isn't ranking #1 on Google enough?</dt>
          <dd>Not when buyers ask LLMs for a verdict. AEO puts you where the <strong>decision</strong> happens.</dd>

          <dt>How fast can we see movement?</dt>
          <dd>Usually <strong>2–6 weeks</strong> if you have any authority. You'll get <strong>before/after screenshots</strong> and booked-meeting stats.</dd>
        </dl>
      </div>
    </section>

    <section class="section" style="border-bottom:none">
      <div class="wrap">
        <div class="card" style="display:flex;flex-direction:column;align-items:center;text-align:center;gap:24px;padding:48px">
          <h2 class="h2" style="margin:0">Be the cited source. Ship more, everywhere. Turn attention into meetings.</h2>
          <div class="divider" style="width:100%;max-width:600px"></div>
          <div class="bar" style="margin:0">
            <a href="#services" class="btn btn-primary" data-cta>Talk to us</a>
            <a href="/guarantees" class="btn btn-secondary">See our Guarantees</a>
          </div>
        </div>
      </div>
    </section>

    <footer class="footer">
      <div class="wrap footer-flex">
        <small>© {new Date().getFullYear()} Become The Answer</small>
        <nav>
          <a href="#services">Services</a>
          <a href="#pricing">Pricing</a>
          <a href="/guarantees">Guarantees</a>
        </nav>
      </div>
    </footer>

    <script>
      const ref = document.referrer || '';
      fetch('/api/events', {
        method: 'POST',
        headers: { 'content-type': 'application/json' },
        body: JSON.stringify({
          claim_slug: 'home',
          page: '/',
          referrer: ref,
          event_type: 'session'
        })
      }).catch(() => {});

      document.querySelectorAll('[data-cta]').forEach(el => {
        el.addEventListener('click', () => {
          fetch('/api/events', {
            method: 'POST',
            headers: { 'content-type': 'application/json' },
            body: JSON.stringify({
              claim_slug: 'home',
              page: '/',
              event_type: 'cta_click'
            })
          }).catch(() => {});
        });
      });
    </script>
  </body>
</html>
